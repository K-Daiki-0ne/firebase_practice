import { makeStyles } from '@mui/styles';

const useStyle = makeStyles({
  loginTogglemode: {
    cursor: 'pointer',
    color: '#0000ff'
  },

  loginReset: {
    cursor: 'pointer'
  }
});

export default useStyle;